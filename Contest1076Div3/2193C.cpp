#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <bits/stdc++.h>
using namespace std;

int main(){
  int t;
  cin >> t;
  

  while(t--){
    
    
    int n , q;
    cin >> n >> q;

    vector<int> a(n);
    vector<int> b(n);
    int l , r;
    cin >> l >> r;

    for(int i= 0; i < n; i++){
      cin >> a[i];
      cin >> b[i];
    }



    int l = 0;
    int r =0;
    
    for(int i = l; l <)
   

    while(sum < s){
      sum += x;
    }

    
    if(sum == s){
      cout << "YES" << endl;
    }

    else {
      cout << "NO" << endl;
    }

    
}
}

