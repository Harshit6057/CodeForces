#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
#include <bits/stdc++.h>
using namespace std;

int main(){
  int t;
  cin >> t;
  

  while(t--){
    
    
    int n , q;
    cin >> n >> q;

    vector<int> a(n);
    vector<int> b(n);
    int l , r;
    cin >> l >> r;

    for(int i= 0; i < n; i++){
      cin >> a[i];
      cin >> b[i];
    }

    for(int i= 0; i < q; i++){
      cin >> l;
      cin >> r;
      
    }

    





    int l = 0;
    int r = 0;
    
    
   

    while(sum < s){
      sum += x;
    }

    
    if(sum == s){
      cout << "YES" << endl;
    }

    else {
      cout << "NO" << endl;
    }

    
}
}

